# Plan — Kebi Native App (`apps/mobile`)

Native React Native build of Kebi. Worked in small, independently-verifiable steps:
do one, verify, move on. No batched tasks.

## Locked decisions (2026-05-29)

- **Source of truth:** `docs/api-contract.md`. Everything else (shared types, design system, `architecture.md`) is aligned to it.
- **Web is dead (parked):** `apps/web` + `libs/ui` + Capacitor shell parked via `.nxignore`, NOT deleted. Delete for real once mobile reaches parity.
- **Placement:** new `apps/mobile` Expo (managed) app in the Nx monorepo, beside `apps/web`.
- **Reuse:** `libs/shared` (after cleanup). NestJS gateway (`services/api`) + kebi (FastAPI) untouched.
- **Auth:** Clerk via `@clerk/clerk-expo`. Gateway contract unchanged (`X-Gateway-User-Id`, `movement_profile` from `publicMetadata`).
- **Styling:** NativeWind (Tailwind class syntax in RN).
- **Primitives:** new `libs/ui-native` lib (parallel to `libs/ui`; `libs/ui` is Radix/DOM and does not port).
- **Design source of truth:** `docs/kebi-app-design-system/` — phone-first 390×844, token-only, lowercase copy.
- **API contract:** `docs/api-contract.md` — gateway-mediated, errors are HTTP 200 with `type: "error"`.

## Design Fidelity Rule

**Every screen built in `apps/mobile` must match its mockup PNG/HTML exactly. No creative decisions in code. If it's in the PNG it ships; if it's not in the PNG it doesn't exist yet — write a note in this file before inventing anything.**

Design source: `docs/kebi-app-design-system/` (HTML = spec, PNG = reference render).  
`design-system.md` governs behavior; `kebi-tokens-mockup.html` governs visuals; `libs/shared` consts (`CATEGORY_EMOJI`, `CATEGORY_EMOJI_FALLBACK`, `SOURCE_LABEL`) govern data bindings. Conflicts: token HTML > design-system.md > shared consts.

### Screens and their mockup files

| Screen | Light mockup | Dark mockup | Track |
|---|---|---|---|
| Splash | `kebi-splash-mockup.html` | `kebi-splash-dark-mockup.html` | C |
| Loading | `kebi-loading-mockup.html` | `kebi-loading-dark-mockup.html` | C |
| Login | `kebi-login-mockup.html` | `kebi-login-dark-mockup.html` | C |
| Home | `kebi-home-mockup.html` | `kebi-home-dark-mockup.html` | C |
| Chat / Consult | `kebi-chat-mockup.html` | `kebi-chat-dark-mockup.html` | C |
| Save sheet (filled) | `kebi-save-sheet-mockup.html` | `kebi-save-sheet-dark-mockup.html` | C |
| Save sheet (empty) | `kebi-save-sheet-empty-mockup.html` | `kebi-save-sheet-empty-dark-mockup.html` | C |
| Save sheet (saving) | `kebi-save-sheet-saving-mockup.html` | `kebi-save-sheet-saving-dark-mockup.html` | C |
| Library | `kebi-library-mockup.html` | `kebi-library-dark-mockup.html` | C |
| Place | `kebi-place-mockup.html` | `kebi-place-dark-mockup.html` | C |
| Settings | `kebi-settings-mockup.html` | `kebi-settings-dark-mockup.html` | C |
| Plans | `kebi-plans-mockup.html` | `kebi-plans-dark-mockup.html` | C |
| Billing | `kebi-billing-mockup.html` | `kebi-billing-dark-mockup.html` | C |
| Toasts | `kebi-toasts-mockup.html` | `kebi-toasts-dark-mockup.html` | B |
| Token reference | `kebi-tokens-mockup.html` | `kebi-tokens-dark-mockup.png` | — |

### Design tokens (from `kebi-tokens-mockup.html` — must wire exactly in A2)

**Light palette:** bg `#FAFAF9` · surface `#F1F1EF` · surface-2 `#E8E8E5` · text `#1A1A1A` · text-muted `#8A8A87` · text-soft `#B5B5B1` · success `#4A7C3A` · like `#C2410C` · warn `#B45309` · warn-bg `#FEF3C7` · new-bg `#FEE4D4` · new-fg `#C2410C` · danger (red)

**Dark palette:** bg `#13110F` · surface `#1C1A17` · surface-2 `#26231F` · text `#F5F1EA` · text-muted `#9A938A` · text-soft `#6E6862` · success `#A8C293` · like `#E8B894` · warn `#E5C078` · pill-green-bg `#1F2E1A` · pill-warm-bg `#3A1F12` · pill-amber-bg `#3A2A0E`

**Typography (Inter only):** 11px·600 (labels/pills) · 12px (legal/small) · 13px (meta/section) · 14px (body) · 15px (standard rows) · 17px (content/chat) · 19px (auth descriptor) · 28px (splash headline) · 32px (chat heading) · 38px (home hero) · 44px (login brand)

**Spacing scale (fixed — no in-between values):** 4 · 8 · 12 · 14 · 16 · 24 · 32

**Radii:** 4px (tag chips) · 7px (avatars) · 10px (buttons, rows) · 12px (cards, sheets) · 14px (group containers) · 999px (pills, AI button)

**Easing (4 curves only):**
- `ease-out` 200–280ms — most state changes
- `ease-in-out` 240ms — symmetric open/close
- `cubic-bezier(0.34, 1.56, 0.64, 1)` 280ms+ — entrances, springs
- `linear` 1.4s — skeleton shimmer only

### RN-specific implementation notes (gaps between HTML and React Native)

These are things that work trivially in CSS but need explicit wiring in RN. Each must be verified before the track step is marked done.

**A2 — Tokens:**
- CSS custom properties → NativeWind theme config + StyleSheet constants object. No inline hex values anywhere.
- `.dark` class toggle → `useColorScheme()` + NativeWind dark variant (`dark:bg-surface`). Single source; components stay theme-agnostic.
- Inter font must be loaded via `expo-font` / `@expo-google-fonts/inter` before first render (part of splash).

**A3 — Navigation:**
- No tab bar. Expo Router file-based routing for screen stack only.
- Safe area: 50px top / 34px bottom — `useSafeAreaInsets()` from `react-native-safe-area-context`.
- AI button (56px circle, 16px from bottom-right edge): absolute-positioned overlay on every screen except Chat. Not a tab bar item. Not present on Chat screen.
- Top-bar pattern: left icon-btn (back/close) + right top-pill. Pill contents differ per screen (see `design-system.md` Standard pill contents table).

**B — Animations:**
- `cubic-bezier(0.34, 1.56, 0.64, 1)` → use `react-native-reanimated` `withSpring` with matching stiffness/damping. Verify visually against mockup PNG.
- Skeleton shimmer → `react-native-reanimated` `LinearGradient` animated position, `linear` 1.4s.
- Reasoning step stagger → `withDelay(index * 350, withTiming(...))`.
- Save sheet spring → `useAnimatedStyle` + `withSpring` 320ms matching `cubic-bezier(0.34, 1.4, 0.64, 1)`.
- Mascot breathing (2.4s ease-in-out scale 1→1.02 + translateY -2px) → `withRepeat(withTiming(...), -1, true)`.
- Toast entrance spring (`cubic-bezier(0.34, 1.56, 0.64, 1)` 280ms) + auto-dismiss 3s/5s → `withSpring` + `useTimeout`.

**B — Components (specific RN gaps):**
- Swipe-to-delete row: `react-native-gesture-handler` `Swipeable` or `PanGestureHandler`. Threshold 40px. One row open at a time — shared ref.
- Place avatar: `Text` with emoji, `28` or `36` size square, `var(--surface-2)` background. No image. `CATEGORY_EMOJI[categories[0]] ?? CATEGORY_EMOJI_FALLBACK`.
- Save sheet: `Modal` or bottom sheet library (e.g. `@gorhom/bottom-sheet`). 24px top radius only. Backdrop scrim: View with opacity 0.45/0.55 + 2px `blurRadius` on `BlurView` (or equivalent).
- Overflow menu: `Modal` with absolute positioning 80px from top, 16px from right. Transparent backdrop catches taps.
- Group container: `View` with `borderRadius: 14`, background surface. Dividers between rows: 1px `View` in surface-2 color between siblings (not on last child).
- Toast pill: absolute `View`, 92px from bottom (above AI button). `Animated.View` for stack.

**C — Screens (copy rules — non-negotiable):**
- All copy is lowercase except proper nouns and brand names.
- "Kebi" is always capitalised; all other UI labels are lowercase.
- No "delete" → "nuke" or "remove". No "settings" hero → "you" or "you, basically". No "recommendations" → "what kebi picked". No "premium" → plan name. No "welcome back!". No "please" / "thank you".
- Meta separators: `·` (middle dot U+00B7), not dash.
- Source attribution: icon + handle only, never "saved from @handle".

**C — Loading / performance budgets:**
- App boot → splash visible: under 1 second.
- Splash → home: 2.5s max.
- No spinners anywhere. Skeleton only.
- Consult streams progressively via SSE — never blocks.

### What is NOT in the mockups (don't build without a new mockup + ADR)

- Any screen not listed in the table above.
- Any color not in the token palette.
- Any spacing value not in the fixed scale (4/8/12/14/16/24/32).
- A second primary button on any screen.
- A tab bar or bottom navigation bar.
- The AI button on the chat screen.
- Letter avatars (deprecated; emoji only).
- "vibe check" copy (deprecated; use "approve?").

---

## Track 0 — Cleanup & alignment (FIRST; small steps, verify each)

Lots of dead code exists. `api-contract.md` is the source of truth; align to it.

- [x] 0.1. Park web: `.nxignore` for `apps/web` + `libs/ui`. Verified: `nx show projects` lists only api/api-e2e/shared; `nx run-many -t lint` targets 3 projects (passes); `shared` typecheck green.
- [x] 0.2. Stripped stale generation from `libs/shared/types.ts` + `sse.ts`; dropped `tool_name`; added live `ChatResponse`/`ToolResult`/`ConsultResult`/`ConsultCandidate`. Verified: `shared` typecheck+lint+test green; grep finds no stale symbols. NOTE: `api` typecheck has **pre-existing** failures (missing `express` types, TypeORM strict-init, stale dist) unrelated to this change — `api` imports none of the removed symbols. Flagged as separate follow-up.
- [x] 0.3. Aligned `design-system.md` bindings: `CATEGORY_EMOJI[place.categories[0]]` + `CATEGORY_EMOJI_FALLBACK`; rewrote Sources→labels to per-save provenance (`user_places.source_label` / `place_name_aliases[].source`), no `place.source`. Verified: no stale singular bindings; named fields exist in `PlaceCore`/`api-contract.md`.
- [x] 0.4. Relocated map to `libs/shared/src/lib/category-emoji.ts` as `CATEGORY_EMOJI: Record<PlaceCategory,string>` + `CATEGORY_EMOJI_FALLBACK` + `SOURCE_LABEL`; exported from index; repointed design-system Source of Truth. Verified: exhaustive `Record` typechecks (1:1 proof), lint+test green.
- [x] 0.5. Updated `architecture.md`: data flow (chat vs extract paths, header auth, `movement_profile` injection), replaced intent taxonomy with `agent|error` + tool_results, expanded endpoint table to all six, fixed DB table names (diagram + Database Ownership). Verified: no stale tokens remain; all `embeddings` hits are `place_embeddings` or capability text.

## Track A — Foundation (sequential; each unblocks the next)

- [ ] A1. Scaffold `apps/mobile` (Nx Expo). Verify: boots in iOS sim, renders a value off `PlaceCore` from `@kebi-app/shared`.
- [x] A2. Port design tokens → RN theme + `.dark` switch (colors, spacing 4/8/12/14/16/24/32, radii, 4 easing curves). Verified: NativeWind v4 + Tailwind v3.4, CSS-var dark mode, Inter font, token-preview screen; typecheck+lint+test green. Dark mode toggle requires `npx expo start --clear` after the config change.
  - **OPEN PROBLEM — responsive scaling (tracked separately):** the design feels right only at the 390pt baseline; on other widths fixed px drift. A `vw` + `scale()` proportional system was prototyped during A3 and **rolled back** (user wants a simpler single-knob approach). Options weighed: (a) fixed px, no scaling — simplest, ~±8% drift across iPhones; (b) one root transform-scale canvas — author in design px, but RN fights safe-areas, navigation, and modals/sheets/toasts render in separate native windows so they escape the wrapper; (c) `vw` tokens + `scale()` helper — robust + crisp but spread across config + helper. To be solved as a dedicated task. Until then, `apps/mobile` uses **fixed design px**.
- [x] A3. Navigation shell — Expo Router (`src/app/`, `root` plugin option), safe areas, top-bar pattern + floating AI button (no tab bar). Routes: home/chat/library/place/settings/plans/billing (splash/login/loading deferred to A5). App-local primitives in `src/components/` (Mascot SVG, token-driven Icon set, IconButton/TopPill/TopBar, KebiFab, ScreenScaffold) ported pixel-exact from `kebi-tokens-mockup.html` §09/§15/§17/§18. FAB on every screen except chat; chat has the mascot title-pill. Verified: headless `expo export -p ios` bundles all 1697 modules + resolves every route (validated the `root: ./src/app` resolution under Nx + Metro package-exports); typecheck + lint + `nx test mobile` (home smoke test, expo-router mocked) all green. Manual iOS-sim tap-through confirmed on device.
- [ ] A4. HTTP transport — port `apps/web/src/api/transports` Strategy to RN fetch. Verify: `GET /v1/health` via gateway returns ok.
- [ ] A5. Clerk-expo auth + session persistence. Verify: sign in → authenticated gateway call accepted.

## Track B — UI primitives in `libs/ui-native` (one component per step, verified vs mockup)

- [ ] group container · status pill · button · AI button (FAB) · toast · chip · place avatar · save sheet · swipe-to-delete row · overflow menu · reasoning block

## Track C — Screens (one mockup + flow + endpoint per step; interleaves with B)

- [ ] splash → login → home → save sheet (`POST /v1/extract`) → chat/consult (`POST /v1/chat/stream` SSE) → library → place → settings → plans/billing
