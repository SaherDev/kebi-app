# Plan — Kebi Native App (`apps/mobile`)

Native React Native build of Kebi. Worked in small, independently-verifiable steps:
do one, verify, move on. No batched tasks.

## Locked decisions (2026-05-29)

- **Source of truth:** `docs/api-contract.md`. Everything else (shared types, design system, `architecture.md`) is aligned to it.
- **Web is dead (parked):** `apps/web` + `libs/ui` + Capacitor shell parked via `.nxignore`, NOT deleted. Delete for real once mobile reaches parity.
- **Placement:** new `apps/mobile` Expo (managed) app in the Nx monorepo, beside `apps/web`.
- **Reuse:** `libs/shared` (after cleanup). NestJS gateway (`services/api`) + kebi (FastAPI) untouched.
- **Auth:** Clerk via `@clerk/clerk-expo`. Gateway contract unchanged (`X-Gateway-User-Id`, `movement_profile` from `publicMetadata`).
- **Styling:** NativeWind (Tailwind class syntax in RN).
- **Primitives:** new `libs/ui-native` lib (parallel to `libs/ui`; `libs/ui` is Radix/DOM and does not port).
- **Design source of truth:** `docs/kebi-app-design-system/` — phone-first 390×844, token-only, lowercase copy.
- **API contract:** `docs/api-contract.md` — gateway-mediated, errors are HTTP 200 with `type: "error"`.

## Track 0 — Cleanup & alignment (FIRST; small steps, verify each)

Lots of dead code exists. `api-contract.md` is the source of truth; align to it.

- [x] 0.1. Park web: `.nxignore` for `apps/web` + `libs/ui`. Verified: `nx show projects` lists only api/api-e2e/shared; `nx run-many -t lint` targets 3 projects (passes); `shared` typecheck green.
- [x] 0.2. Stripped stale generation from `libs/shared/types.ts` + `sse.ts`; dropped `tool_name`; added live `ChatResponse`/`ToolResult`/`ConsultResult`/`ConsultCandidate`. Verified: `shared` typecheck+lint+test green; grep finds no stale symbols. NOTE: `api` typecheck has **pre-existing** failures (missing `express` types, TypeORM strict-init, stale dist) unrelated to this change — `api` imports none of the removed symbols. Flagged as separate follow-up.
- [x] 0.3. Aligned `design-system.md` bindings: `CATEGORY_EMOJI[place.categories[0]]` + `CATEGORY_EMOJI_FALLBACK`; rewrote Sources→labels to per-save provenance (`user_places.source_label` / `place_name_aliases[].source`), no `place.source`. Verified: no stale singular bindings; named fields exist in `PlaceCore`/`api-contract.md`.
- [x] 0.4. Relocated map to `libs/shared/src/lib/category-emoji.ts` as `CATEGORY_EMOJI: Record<PlaceCategory,string>` + `CATEGORY_EMOJI_FALLBACK` + `SOURCE_LABEL`; exported from index; repointed design-system Source of Truth. Verified: exhaustive `Record` typechecks (1:1 proof), lint+test green.
- [x] 0.5. Updated `architecture.md`: data flow (chat vs extract paths, header auth, `movement_profile` injection), replaced intent taxonomy with `agent|error` + tool_results, expanded endpoint table to all six, fixed DB table names (diagram + Database Ownership). Verified: no stale tokens remain; all `embeddings` hits are `place_embeddings` or capability text.

## Track A — Foundation (sequential; each unblocks the next)

- [ ] A1. Scaffold `apps/mobile` (Nx Expo). Verify: boots in iOS sim, renders a value off `PlaceCore` from `@kebi-app/shared`.
- [ ] A2. Port design tokens → RN theme + `.dark` switch (colors, spacing 4/8/12/14/16/24/32, radii, 4 easing curves). Verify: token-preview screen toggles light/dark.
- [ ] A3. Navigation shell — Expo Router, safe areas, top-bar pattern + floating AI button (no tab bar). Verify: navigate between stub screens.
- [ ] A4. HTTP transport — port `apps/web/src/api/transports` Strategy to RN fetch. Verify: `GET /v1/health` via gateway returns ok.
- [ ] A5. Clerk-expo auth + session persistence. Verify: sign in → authenticated gateway call accepted.

## Track B — UI primitives in `libs/ui-native` (one component per step, verified vs mockup)

- [ ] group container · status pill · button · AI button (FAB) · toast · chip · place avatar · save sheet · swipe-to-delete row · overflow menu · reasoning block

## Track C — Screens (one mockup + flow + endpoint per step; interleaves with B)

- [ ] splash → login → home → save sheet (`POST /v1/extract`) → chat/consult (`POST /v1/chat/stream` SSE) → library → place → settings → plans/billing
